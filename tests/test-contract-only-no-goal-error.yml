name: Test contract-only without goal fails
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          goal: ''
          contract-only: 'true'
        continue-on-error: true
      - name: Assert action failed with missing goal error
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail when contract-only=true and goal is empty, but outcome was: $OUTCOME"
            exit 1
          fi
          echo "PASS: action correctly failed when contract-only=true with no goal"
